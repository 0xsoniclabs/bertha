[package]
name = "genesis-parser"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
# bertha
bertha-types = { path = "../bertha-types" }

# serialization
alloy-rlp = { version = "0.3.12", features = ["derive"] }

# compression
flate2 = "1.1.1"

# error handling
thiserror = "2.0.12"

[dev-dependencies]
const-hex = "1.14.1"

[lints]
workspace = true
